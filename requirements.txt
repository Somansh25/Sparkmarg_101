# Core Web Framework & Server
flask[async]==3.1.3
flask-cors==6.0.5
gunicorn==26.0.0
werkzeug>=3.0.0

# Database
pymongo[srv]==4.17.0

# Validation & Settings
pydantic[email]==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# Security & Authentication
python-jose[cryptography]==3.5.0
python-dotenv==1.2.2

# CHANGED: Pruned unused/redundant security packages (bcrypt, passlib, PyJWT) to eliminate security attack surface and reduce installation footprint.